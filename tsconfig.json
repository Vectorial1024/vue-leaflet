{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    /* Common type-checking */
    // vite provides types but is not under @types, so their types cannot be discovered
    // note: also see https://vite.dev/guide/features.html#client-types
    "types": ["node", "vite/client"]
  }
}
